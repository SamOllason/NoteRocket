@page "/notes"

<h1>Notes</h1>


<ul>
    @foreach(var note in notes)
    {
        <li>
            @note.Title
        </li>
    }
</ul>

@code {
    private List<Note> notes = NotesRepository.GetNotes();
}
